packages:
  pip:
    - taskr
    - boto
  brew:
    - youtube-dl
    - test: which python3
      package: python3
  yum:
    - wget
  gem:
    - cocoapods

installers:
  brew:
    sudo: false
    os: true
    multiple: true
    syntax: "{} install {}"
    command: "brew"
